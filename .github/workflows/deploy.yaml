name: 'Deploy app'

on:
  push:
    branches:
      - "master"

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:

      # Checkout the repo
      - uses: actions/checkout@master

      - name: Echo
        run: echo $(date)

  deploy-dev:
    runs-on: 'ubuntu-latest'
    environment: 'dev'
    needs: build-and-publish
    continue-on-error: true
    steps:
      - name: Deploy dev
        run: echo Deploy Dev...
      - name: Step 3
        if: ${{ failure() && steps.demo.conclusion == 'failure' }}
        run: echo "And NOW this step will run"

  deploy-prod:
    runs-on: 'ubuntu-latest'
    environment: 'prod'
    needs: [build-and-publish, deploy-dev]
    steps:
      - name: Deploy prod
        run: echo Deploy Prod...